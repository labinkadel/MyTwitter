<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labin.mytwitter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeLight">
        <activity android:name=".TwiteerDashActivity">
        </activity>
        <activity
            android:name=".activity.Dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppThemeLight.NoActionBar" />
        <activity android:name=".ui.share.ShareFragment" />
        <activity android:name=".ui.send.SendFragment" />
        <activity android:name=".ui.gallery.GalleryFragment" />
        <activity android:name=".activity.verifiActivity" />
        <activity android:name=".activity.FinalRegActivity" />
        <activity android:name=".activity.AggActivity" />
        <activity android:name=".activity.PasswordActivity" />
        <activity android:name=".activity.CameraActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.CreateAccountActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activity.ComposeTweetActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activity.EditTweetActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>